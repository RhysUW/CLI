SaaSquatch CLI is a simple command-line application written in Java using the Picocli library. It is designed to greet SaaSquatch with a friendly "Hello SaaSquatch!" message.

Building:
To build the application, use:

./gradlew build

Usage
Run the CLI using Gradle:

./gradlew hello